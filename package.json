{
    "name": "@private-repo/cloudrun-express-sql",
    "version": "1.0.0",
    "description": "Express app deployed to Cloud Run with Cloud SQL Postgres",
    "main": "index.mjs",
    "type": "module",
    "scripts": {
        "start": "node index.mjs",
        "dev": "nodemon index.mjs",
        "test": "echo \"No tests yet\" && exit 0",
        "artifactregistry-login": "npx google-artifactregistry-auth --repo-config=.npmrc"
    },
    "dependencies": {
        "@google-cloud/cloud-sql-connector": "^1.5.0",
        "ci": "^2.3.0",
        "express": "^4.19.2",
        "pg": "^8.11.5"
    },
    "devDependencies": {
        "google-artifactregistry-auth": "^3.4.0",
        "nodemon": "^3.1.0"
    }
}
